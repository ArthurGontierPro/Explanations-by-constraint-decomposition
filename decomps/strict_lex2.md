# strict_lex2

See `decomps/lex2.md` (covers `lex2`, `strict_lex2`, `lex2_strict` together — `CHRISTMAS_LIST.md`
§3 lists them as one entry). Same shape, `lex_less` instead of `lex_lesseq` per row pair.
