# lex2_strict

See `decomps/lex2.md`. Possibly a duplicate name for `strict_lex2` (`decomps/strict_lex2.md`)
in MiniZinc's own stdlib — not independently confirmed, flagged rather than asserted; either
way, same shape as `lex2.md`.
