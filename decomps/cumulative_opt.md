# cumulative_opt

**Signature.** As `cumulative`, with optional tasks.

**Shape: `SCH-2` + modifier `M-opt`.** Differs from `cumulative` only by the `Ex_i` conjunct in
`B2_{i,t}`, exactly as `disjunctive_opt` differs from `disjunctive`.

**E-code: E2 + E4** (`CHRISTMAS_LIST.md` §6 gives `cumulative_opt` E2 + E4 alongside
`cumulatives`), plus **E1** for the `Ex_i` family name (gap **G2**) and gaps **X4**, **X5**.
Strictly harder than `cumulative`, which is already blocked on E4.
