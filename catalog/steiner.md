<!-- AUTO-STUB — NOT REVIEWED. Machine-generated by python3 tools/catalog_stub.py; see the banner below. -->

# `steiner`

> **AUTO-STUB — NOT REVIEWED.**
> Generated 2026-09-21 by `python3 tools/catalog_stub.py`.
> Every field below is machine-derived and names the source it came from. Every
> field that would take judgement reads `not reviewed`, and that means **nobody has
> read this constraint** — not that there is nothing to say about it. This file
> exists so the catalog's denominator is honest; it is **not** evidence of work on
> `steiner`, and `catalog/INDEX.md` counts stubs and reviewed entries
> separately for that reason.
> **To upgrade it:** edit in place against `catalog/TEMPLATE.md` — the headings
> below are already in template order — and delete this banner. Deleting the
> banner is also what stops `python3 tools/catalog_stub.py` from ever rewriting the file.

> **This entry lists the rules this method generated for the events it was asked to
> explain. It is not a list of all valid explanations of `steiner`,
> and no claim of that kind is made anywhere in this catalog.** See
> `catalog/README.md`, "What the catalog claims".

| | |
|---|---|
| **Tier** | **out of scope** (`- out of scope`) — `python3 tools/mzn_coverage.py --rank` |
| **Status** | `not reviewed` — no status-legend value has been assigned |
| **Generated** | no generator entry — there is no `cata/steiner.tex` |
| **Validator** | `not reviewed` — `make validate` has not been run for this entry |
| **Calibration** | `not reviewed` |
| **Last measured** | 2026-09-21, `python3 tools/catalog_stub.py` — machine-derived fields only; nothing here was validated |

## Constraint

`not reviewed` — no MiniZinc signature is vendored in this repo.

**Provenance of the name:** `tools/data/minizinc-2.10.1-globals.txt:116` lists `steiner` as a global of
MiniZinc 2.10.1; that snapshot carries names only, never signatures.
`CHRISTMAS_LIST.md:186` files it under section `8. Graph and reachability`.

## Published explanation

**Literature: absent.** The literature column of `CHRISTMAS_LIST.md:186` reads, verbatim:

> none found beyond the circuit line of work

That is a quote of the list, not a reading of a paper: nothing has been fetched,
nothing has been read, and this stub makes no statement about any paper's content.

**Rule shape:** `pending C2` — there is no `catalog/_literature/steiner.md`, so no
published rule shape is stated here. (`catalog/TEMPLATE.md`: never write one
from memory.)

## Solver support

| | |
|---|---|
| Chuffed | native |
| Geas | absent |
| Choco LCG | `[C✗ for `tree`]` failure |

Derived mechanically from the solver column of `CHRISTMAS_LIST.md:186`, which reads, verbatim:

> **native** (`tree.cpp`, `dtree.cpp`, `dag.cpp`, `dconnected.cpp`, `bounded_path.cpp`, `well-founded.cpp`, `EdExplFinder.cpp`) **[C✗ for `tree`]**

Legend: `CHRISTMAS_LIST.md:106-109`. The table above is a `native`/`decomp` keyword
match plus the `[G]`/`[C]` markers and nothing else; anything the legend does not
cover is not represented.

## Decomposition used here

**Generator value:** `not reviewed` — no value in `explenation generator.ml` has
been identified, and nothing under `cata/` is emitted under this name.
**Emitted by:** `not reviewed`
**Spec:** none — this constraint has no `decomps/steiner.md`.

`not reviewed` — the schema chain is review work: it takes reading the
decomposition and saying what it actually encodes, which is exactly the judgement a
stub must not fake.

## Scope of this entry

**Events the generator was asked to explain:** `not reviewed`.

There is no `cata/steiner.tex`, so there is no `%% generator diagnostics (W1-T3)`
footer to read and no event list to state.

## Generated rules

**no generator entry** — there is no `cata/steiner.tex`, so this method has
generated nothing for this constraint. Whether that is a gap, a missing
decomposition or a constraint out of scope is `not reviewed`.

## Status

**`not reviewed`**

No value from `catalog/README.md`'s status legend has been assigned, deliberately:
all six are verdicts, and this file states none. In particular this is **not**
`generated, unvalidated` and **not** a claim that anything is blocked — those are
findings, and nobody has looked.

## Calibration (W3-T5, D-0013)

**Verdict:** `not reviewed` — no comparison against a published rule has been
attempted. Note that `no published rule exists` is itself a verdict and is **not**
being claimed here, even where the literature column above reads `none`.

## Gaps

| gap | what it blocks here |
|---|---|
| `not reviewed` | no gap has been attributed to this constraint |

**E-code route, quoted from `CHRISTMAS_LIST.md:186`** — this is the list's own routing
note, carried across verbatim, and not a verdict of this catalog:

> **E6** — Chuffed has a dedicated *edge explanation finder*, which tells you how far this is from clause unfolding

E-codes parsed out of that cell by `tools/mzn_coverage.py`: `E6`.
Source: `docs/DECOMP_FORMAT_NOTES.md` for what the numbered gaps mean; no gap from it
has been applied here.

## How this entry was produced

- `python3 tools/catalog_stub.py`, 2026-09-21 — this entire file, in one pass.
- `tools/mzn_coverage.py` (imported, not shelled out) → the release global list, the
  priority tier `- out of scope`, and the `CHRISTMAS_LIST.md` row for this name.
- `tools/data/minizinc-2.10.1-globals.txt:116` read → the name and the release it ships in.
- `CHRISTMAS_LIST.md:186` read → the literature, solver and route cells quoted above, verbatim.
- `os.path.isfile("decomps/steiner.md")` → False.
- `os.path.isfile("cata/steiner.tex")` → False.
- Nothing was compiled, run, validated or judged. There is no verdict anywhere in
  this file, because there is no reviewed claim in it to attach one to.
